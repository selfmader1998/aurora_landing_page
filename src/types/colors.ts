import { DARK_MODE, LIGHT_MODE, PALETTE } from "../constants";

type PaletteType = typeof PALETTE;

type LightModeType = typeof LIGHT_MODE;
type DarkModeType = typeof DARK_MODE;

type ThemeType = typeof LIGHT_MODE;

export type { PaletteType, LightModeType, DarkModeType, ThemeType };
